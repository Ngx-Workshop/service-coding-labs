# docker-compose.yml
version: '3.8'

services:
  coding-labs:
    build: .
    container_name: coding-labs
    restart: unless-stopped
    ports:
      - "3009:3009"
    env_file:
      - .env
    networks:
      - ngx-net

networks:
  ngx-net:
    external: true